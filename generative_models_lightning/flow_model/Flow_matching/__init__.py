"""Probability paths, objectives, and solvers for flow-based models.

Use the focused ``path``, ``loss``, ``solver``, and ``utils`` subpackages in
configuration targets so importing this package does not eagerly load every
optional solver dependency.
"""
